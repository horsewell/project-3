-- DROP TABLE IF EXISTS stock_data;

-- CREATE TABLE AMZN (
--     company_id VARCHAR(10) NOT NULL,
-- 	date DATE PRIMARY KEY,
--     close DECIMAL(10,2),
--     volume INTEGER,
-- 	open DECIMAL(10,2),
--     high DECIMAL(10,2),
--     low DECIMAL(10,2)
-- );

-- CREATE TABLE APPL (
--     company_id VARCHAR(10) NOT NULL,
-- 	date DATE PRIMARY KEY,
--     close DECIMAL(10,2),
--     volume INTEGER,
-- 	open DECIMAL(10,2),
--     high DECIMAL(10,2),
--     low DECIMAL(10,2)
-- );

-- CREATE TABLE META (
--     company_id VARCHAR(10) NOT NULL,
-- 	date DATE PRIMARY KEY,
--     close DECIMAL(10,2),
--     volume INTEGER,
-- 	open DECIMAL(10,2),
--     high DECIMAL(10,2),
--     low DECIMAL(10,2)
-- );

-- CREATE TABLE NFLX (
--     company_id VARCHAR(10) NOT NULL,
-- 	date DATE PRIMARY KEY,
--     close DECIMAL(10,2),
--     volume INTEGER,
-- 	open DECIMAL(10,2),
--     high DECIMAL(10,2),
--     low DECIMAL(10,2)
-- );


select * from meta
